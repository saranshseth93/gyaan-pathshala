{
  "name": "gyaan-pathshala/website",
  "description": "Gyaan Pathshala website deployment automation",
  "type": "project",
  "license": "MIT",
  "require": {},
  "scripts": {
    "post-update-cmd": [
      "echo 'Deployment triggered by GitHub webhook.'",
      "npm install --legacy-peer-deps",
      "npm run build",
      "rm -rf ../public_html/* || true",
      "cp -r dist/* ../public_html/"
    ]
  },
  "config": {
    "optimize-autoloader": true,
    "sort-packages": true
  }
}
